# Usage Guide:

With the exception of **functions_setup.py**, the other scripts only run
if the **regresision_analysis_part_a.py** is run. This is due to the 
**np.savez** solutions we used.  

Further, the name of the files should indicate what it does quite precisely.